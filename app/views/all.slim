div#map style="width:100%;height:500px"

javascript:
  function myMap() {

    var poi = [#{poi.coordonnees}]

    var mapCanvas = document.getElementById("map");
    var mapOptions = {zoom: #{zoom.to_i}};
    var map = new google.maps.Map(mapCanvas,mapOptions);

    setMarkers(map);

    var runPath = new google.maps.Map({
      path: [home, a0, a1_0, a1_1, a2, a3, a4, a5],
      strokeColor: "#046380",
      strokeOpacity: 0.8,
      strokeWeight: 3
    });
    runPath.setMap(map);
  }

<script src="https://maps.googleapis.com/maps/api/js?key=#{my_api_key}&callback=myMap"></script>
